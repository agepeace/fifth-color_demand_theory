实施计划如下，按 ResearchLog.md 的“下一步”推进：先完成文档状态统一，再进入 PAPER.md 第 6 节证明细化。

**第一阶段：统一状态措辞与支撑文档**

1. 以 README.md 当前口径作为全仓库标准：
   - “已有闭合的工作性证明链”；
   - “尚未达到最终定理级证明”；
   - 不再把中心命题表述为单纯猜想，也不包装成已完成的最终定理。

2. 修改 papers/PAPER.md 的状态冲突：
   - 第 1 节中把“不是主定理已经闭合证明”改为“已有工作性闭合链，但非最终定理级证明”。
   - 第 6.10 节弱化“正式回收 / 命题成立 / 证毕”的终局语气，统一为“在当前形式化与工作性粗分类下回收”。
   - 第 7 节、第 9 节与结论部分同步改写，避免“当前并未证明该命题”与“已经正式回收中心命题”互相冲突。

3. 修改 Volume1/Fifth-Color_Demand_Theory_Framework.md：
   - 将 “Central Conjecture” 更新为 “Central Working Proposition / Central Target”。
   - 将旧的 `girth ≥ 4` 历史目标收敛到当前 `girth ≥ 5` 版本。
   - 把 “currently a conjecture, not a theorem” 改为“当前已有 PAPER.md 支撑的工作性证明链，但尚未达到最终定理级证明”。
   - 加入色压与广义第五色需求说明，使 Framework 与 PAPER.md 的核心定义同步。

4. 修改 docs/Glossary.md：
   - 添加“色压 / Color Pressure”“广义第五色需求 / Generalized Fifth-Color Demand”“工作性证明链 / Working Proof Chain”“局部刚性 / Local Rigidity”等条目。
   - 将“论文主稿 / Paper Draft”收敛为“研究论文主稿 / Research Paper”，同时注明当前仍是工作性研究论文而非最终定理稿。
   - 给“唯一性主张”补状态说明：当前作为工作性命题处理，由 PAPER.md 第 6 节支撑，尚待微型构型枚举完全化。

5. 修改 docs/Notation.md：
   - 添加 `\pi(v,\varphi)`、`F^{\dagger}(v,\varphi)`、`\mathfrak{P}_{\partial}`、`R_H` 等记号。
   - 将势函数 `\Phi(\varphi)` 从 `\sum F(v,\varphi)` 修正为与 PAPER.md 一致的 `\sum F^{\dagger}(v,\varphi)`，并说明静态版本只是特例或早期接口。
   - 把“单口 / 双口 / 回返”从纯描述标签升级为与接口原型表对应的工作性记号说明。

6. 修改 README.md：
   - 保留当前主体口径，只做术语微调与最后更新时间更新。

**第二阶段：细化 PAPER.md 第 6 节证明接口**

1. 在第 6.6.7 后补入“表 6.6-A（极小见证边界的有限局部模式表）”：
   - 列出单口、双口、回返三类原型的有限模式，例如 `S1`、`D1`、`D2`、`R1`、`R2`。
   - 每个模式记录贴附口数、是否允许回返、禁止退化、后续对应的 I/II/III/IV 类型。

2. 在第 6.9 开头补入“命题 6.9A（从局部模式表到 I-IV 型候选簇）”：
   - 说明 I-IV 型由“两端模式表元素 + 最短路径贴附数据 + 回返优先规则”产生。
   - 同步改写引理 6.9b 的证明，使其引用 6.6.3、6.6.7 与表 6.6-A，而不是重新用较粗的贴附口数量讨论。

3. 在 6.9.4 开头补入“引理 6.9.4-0（回返原型到 IV 型临界面带的桥接）”：
   - 明确说明引理 6.6.6 的回返原型如何在双核连接体中成为 IV 型临界面带。
   - 把 IV 型所用的边界必要性、最短性、不可剪除性直接绑定到 6.6.6 的刚性条件。

4. 给 6.9.1 至 6.9.4 各补一个微型构型枚举表：
   - I 型：回返边、外部分支、外部无贡献邻点。
   - II 型：双口提前合流、并行支路、一路压力失效。
   - III 型：对应关系交换、无代价合流、完全分离成并行带、外部无贡献邻点退化。
   - IV 型：更短弦、外接传播支路、无贡献边界段、扩张段可剪除、重复分配裂解。

5. 修正 IV 型内部的长度表述：
   - 当前 `|\partial R|=4` 与 `g(G)\ge 5` 容易冲突。
   - 优先采用“结构段数”方案，把 IV-S 改为“四个必要边界段”而不是实际边界长度为 4；这样保留原证明语义且改动较小。
   - 同步把引理 6.8 中“内部面边界长度不小于 4”改为“不小于 5”，匹配 `g(G)\ge 5`。

**第三阶段：收尾检查**

1. 通读修改过的段落，检查中文行文、英文状态句、符号引用是否一致。
2. 搜索旧口径：`paper draft`、`Central Conjecture`、`not a theorem`、`正式回收`、`当前并未证明该命题`、`|\partial R|=4` 等。
3. 检查 git diff，确认只改动相关论证文档；不提交、不推送，除非用户另行要求。